name: Build Franime Only

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build Franime only (skip lint)
      run: ./gradlew :src:fr:franime:assembleDebug -x lintKotlin --stacktrace
      
    - name: Collect APK
      run: |
        mkdir -p apk-output
        find src/fr/franime -path "*/build/outputs/apk/debug/*.apk" -exec cp {} apk-output/ \;
        ls -la apk-output/
        
    - name: Upload to Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: franime-apk
        path: apk-output/*.apk
        
    - name: Create Release
      uses: softprops/action-gh-release@v1
      if: github.event_name == 'push'
      with:
        files: apk-output/*.apk
        tag_name: franime-v${{ github.run_number }}
        name: Franime Build ${{ github.run_number }}
        body: |
          Build automatique de l'extension Franime
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}