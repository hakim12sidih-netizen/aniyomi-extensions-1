ext {
    extName = 'FRAnime'
    extClass = '.FRAnime'
    extVersionCode = 1
    // FRAnime uses Cloudflare — FlareSolverr is required
    isNsfw = false
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:playlist-utils"))
}
